# ✅ Чеклист тестирования DocFlow AI

## Статус системы: ✅ ВСЕ СЕРВИСЫ РАБОТАЮТ

Все контейнеры запущены и отвечают на запросы.

---

## Шаг 1: Проверка доступности сервисов

### ✅ Core API (порт 8004)
```bash
curl http://localhost:8004/health
# Ожидается: {"status":"ok"}
```

### ✅ Qdrant (порт 6333)
```bash
curl http://localhost:6333/collections
# Ожидается: {"result":{"collections":[]},...}
```

### ✅ Swagger UI
Откройте в браузере:
- Scraper: http://localhost:8000/docs
- Cleaner: http://localhost:8001/docs
- Normalizer: http://localhost:8002/docs
- Indexer: http://localhost:8003/docs
- **Core API: http://localhost:8004/docs** ⭐

---

## Шаг 2: Тест индексации (быстрый способ)

### Вариант A: Через скрипт (рекомендуется)
```bash
# Убедитесь, что файлы есть в data/docs/
ls data/docs/

# Запустите скрипт индексации
python scripts/quick_index.py test-space "*.txt"
```

### Вариант B: Прямая загрузка в Core API (для тестирования)
```bash
curl -X POST http://localhost:8004/spaces/test-space/ingest \
  -H "Content-Type: application/json" \
  -d '{
    "documents": [
      {
        "external_id": "test:doc1:0",
        "text": "Это тестовый документ для проверки RAG системы. Он содержит информацию о том, как работает индексация и поиск.",
        "metadata": {
          "source": "file",
          "path": "test.txt",
          "title": "Тестовый документ",
          "created_at": "2024-01-01T00:00:00Z",
          "chunk_index": 0,
          "total_chunks": 1
        }
      }
    ]
  }'
```

**Ожидается:** `{"indexed": 1}`

---

## Шаг 3: Проверка индексации в Qdrant

```bash
# Проверить, что коллекция создалась
curl http://localhost:6333/collections

# Должна появиться коллекция "space_test-space"

# Информация о коллекции
curl http://localhost:6333/collections/space_test-space | jq .
```

---

## Шаг 4: Тест RAG-запроса

```bash
curl -X POST http://localhost:8004/spaces/test-space/query \
  -H "Content-Type: application/json" \
  -d '{
    "query": "О чём этот документ?",
    "top_k": 3
  }'
```

**Ожидается:**
```json
{
  "answer": "Ответ LLM на основе документа...",
  "sources": [
    {
      "text": "Это тестовый документ...",
      "source": "file",
      "path": "test.txt",
      "title": "Тестовый документ"
    }
  ]
}
```

**⚠️ Важно:** Если используете Ollama, убедитесь что:
- Ollama запущен локально: `ollama serve`
- Модель установлена: `ollama pull gemma3:4b`
- Или установите OPENAI_API_KEY для использования OpenAI

---

## Шаг 5: Полный пайплайн (через все сервисы)

### 5.1 Scraper - читаем файлы
```bash
curl -X POST http://localhost:8000/api/v1/scrape \
  -H "Content-Type: application/json" \
  -d '{"file_glob": "*.txt"}'
```

### 5.2 Cleaner - очищаем текст
```bash
# Используйте результат из шага 5.1
curl -X POST http://localhost:8001/clean \
  -H "Content-Type: application/json" \
  -d '{
    "items": [
      {
        "source": "file",
        "path": "doc1.txt",
        "content": "текст из scraper"
      }
    ]
  }'
```

### 5.3 Normalizer - разбиваем на чанки
```bash
# Используйте результат из шага 5.2
curl -X POST http://localhost:8002/normalize \
  -H "Content-Type: application/json" \
  -d '{
    "items": [
      {
        "source": "file",
        "path": "doc1.txt",
        "raw_content": "текст",
        "cleaned_content": "текст"
      }
    ]
  }'
```

### 5.4 Indexer - отправляем в Core API
```bash
# Используйте результат из шага 5.3
curl -X POST http://localhost:8003/index/test-space \
  -H "Content-Type: application/json" \
  -d '{
    "items": [
      {
        "external_id": "file:doc1.txt:0",
        "text": "текст чанка",
        "metadata": {...}
      }
    ]
  }'
```

---

## Шаг 6: Проверка через Swagger UI

1. Откройте http://localhost:8004/docs
2. Найдите endpoint `POST /spaces/{space_id}/ingest`
3. Нажмите "Try it out"
4. Введите `space_id`: `test-space`
5. Вставьте JSON с документом (см. Шаг 2, Вариант B)
6. Нажмите "Execute"
7. Проверьте ответ: должно быть `{"indexed": 1}`

---

## Возможные проблемы и решения

### ❌ Ошибка: "Connection refused" при запросе к API
**Решение:** Проверьте, что контейнер запущен:
```bash
docker compose ps api
```

### ❌ Ошибка: "Failed to index documents" 
**Решение:** Проверьте логи:
```bash
docker compose logs api
```

### ❌ Ошибка: "Query failed" при запросе
**Решение:** 
1. Проверьте, что документы проиндексированы (Шаг 3)
2. Проверьте настройки LLM (Ollama запущен или OPENAI_API_KEY установлен)
3. Проверьте логи:
```bash
docker compose logs api | tail -20
```

### ❌ Qdrant не отвечает
**Решение:**
```bash
docker compose logs qdrant
docker compose restart qdrant
```

---

## Следующие шаги после успешного тестирования

1. ✅ Индексация работает
2. ✅ RAG-запросы работают
3. 📝 Можно начинать добавлять реальные данные
4. 📝 Настроить мониторинг (если нужно)
5. 📝 Добавить аутентификацию (Неделя 3)

---

## Быстрая проверка всего за 30 секунд

```bash
# 1. Индексация
curl -X POST http://localhost:8004/spaces/quick-test/ingest \
  -H "Content-Type: application/json" \
  -d '{"documents":[{"external_id":"test:1","text":"Тест","metadata":{"source":"file","path":"test.txt","title":"Test","created_at":"2024-01-01T00:00:00Z","chunk_index":0,"total_chunks":1}}]}'

# 2. Запрос
curl -X POST http://localhost:8004/spaces/quick-test/query \
  -H "Content-Type: application/json" \
  -d '{"query":"О чём документ?","top_k":1}'
```

Если оба запроса вернули успешные ответы - всё работает! 🎉

