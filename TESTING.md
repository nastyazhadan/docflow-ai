# 🧪 Руководство по запуску тестов

## Быстрый старт

### 1. Запустить все сервисы (если остановлены)
```bash
docker compose up -d
```

### 2. Проверить статус
```bash
docker compose ps
```

Все сервисы должны быть в статусе `Up`.

---

## Запуск тестов

### Тесты отдельных сервисов

#### Scraper Service
```bash
cd services/scraper
pytest tests/
```

#### Cleaner Service
```bash
cd services/cleaner
pytest tests/
```

#### Normalizer Service
```bash
cd services/normalizer
pytest tests/
```

#### Indexer Service
```bash
cd services/indexer
pytest tests/
```

### Интеграционные тесты (полный пайплайн)

```bash
cd services/tests_integration
pytest test_ingestion_pipeline.py -v
```

**Важно:** Интеграционные тесты проверяют весь пайплайн:
- Scraper → Cleaner → Normalizer → Indexer → Core API

### Все тесты сразу

Из корня проекта:
```bash
# Тесты всех сервисов
pytest services/*/tests/ -v

# Интеграционные тесты
pytest services/tests_integration/ -v
```

---

## Структура тестов

```
services/
├── scraper/
│   └── tests/
│       ├── api/v1/test_endpoints.py      # Тесты API endpoints
│       └── services/
│           ├── test_file_reader.py      # Тесты чтения файлов
│           └── test_http_fetcher.py     # Тесты HTTP запросов
│
├── cleaner/
│   └── tests/
│       └── services/test_cleaner.py     # Тесты очистки текста
│
├── normalizer/
│   └── tests/
│       └── services/test_normalizer.py  # Тесты нормализации
│
├── indexer/
│   └── tests/
│       └── services/test_indexer.py     # Тесты индексации
│
└── tests_integration/
    └── test_ingestion_pipeline.py      # Полный пайплайн
```

---

## Примеры запуска

### Запустить тесты с выводом
```bash
pytest -v
```

### Запустить конкретный тест
```bash
pytest services/scraper/tests/services/test_file_reader.py::test_read_all -v
```

### Запустить с покрытием
```bash
pytest --cov=scraper_service services/scraper/tests/
```

### Запустить только быстрые тесты
```bash
pytest -m "not slow"
```

---

## Требования для тестов

1. **Виртуальное окружение активировано:**
   ```bash
   source venv/bin/activate
   ```

2. **Зависимости установлены:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Для интеграционных тестов:**
   - Сервисы должны быть доступны (через TestClient, не требуют Docker)
   - Тесты используют моки для Core API

---

## Проверка перед запуском тестов

```bash
# 1. Активировать venv
source venv/bin/activate

# 2. Проверить установку pytest
pytest --version

# 3. Запустить быстрый тест
pytest services/scraper/tests/services/test_file_reader.py -v
```

---

## Устранение проблем

### Ошибка импорта модулей
```bash
# Убедитесь, что вы в правильной директории
cd services/scraper
pytest tests/
```

### Ошибка "ModuleNotFoundError"
```bash
# Установите зависимости
pip install -r requirements.txt
```

### Интеграционные тесты не работают
```bash
# Проверьте, что пути к сервисам настроены в conftest.py
# Тесты автоматически добавляют пути через sys.path
```

