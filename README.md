docker compose build
docker compose up

Коротко: дальше по ветке B идти не надо, пока не поднят хотя бы скелет Core API (ветка A, недели 1–2). Лучше сейчас зафризить B после рабочей ingestion-цепочки и переключиться на A.

Как только у тебя есть:

живой /spaces/{id}/ingest, который реально пишет в векторку,

минимальный /query, который что-то возвращает по этим данным,

— B-ветку можно легко “дострелить” до полноценного демо: indexer начнёт реально индексировать, а воркфлоу-галерея будет вызывать уже настоящий RAG.

Итого: смысла «убегать вперёд» по B сейчас мало — почти всё там завязано на том, чего в A ещё нет.
