# DocFlow AI

Система индексации документов и RAG (Retrieval-Augmented Generation) на основе LlamaIndex и Qdrant.

Запустить web:

 ```bash
 cd ui
 streamlit run .\web_app.py
 ```

## ⚠️ Важно: Требования перед запуском

**Система работает только локально с Ollama. Перед запуском необходимо:**

1. **Установить Ollama:**
    - Скачайте с https://ollama.com/
    - Установите и запустите: `ollama serve`

2. **Установить необходимые модели:**
   ```bash
   # Модель для LLM (генерация ответов)
   ollama pull gemma3:4b
   
   # Модель для embeddings (векторные представления)
   ollama pull nomic-embed-text
   ```

3. **Проверить, что Ollama работает:**
   ```bash
   curl http://localhost:11434/api/tags
   ```

Без установленного и запущенного Ollama система не будет работать!

---

## Быстрый старт

```bash
# 1. Установите Ollama и модели (см. выше)

# 2. Запустите все сервисы
docker compose up -d

# 3. Проверьте статус
docker compose ps

# 4. Откройте Swagger UI
# Core API: http://localhost:8004/docs
```

---

## Индексация документов

### Вариант 1: Через скрипт (рекомендуется)

```bash
# Положите файлы в data/docs/
echo "Ваш текст" > data/docs/my_doc.txt

# Запустите индексацию
python scripts/quick_index.py my-space "*.txt"
```

### Вариант 2: Через API

См. `INGESTION_GUIDE.md` для подробных инструкций.

---

## Запросы к RAG системе

```bash
curl -X POST http://localhost:8004/spaces/my-space/query \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Ваш вопрос",
    "top_k": 5
  }'
```

Или через Swagger UI: http://localhost:8004/docs

---

## Архитектура

```
Файлы/URL → Scraper → Cleaner → Normalizer → Indexer → Core API → Qdrant
                                                                    ↓
                                                              RAG Queries
```

---

## Статус проекта

✅ **Недели 1-2 выполнены:**

- Core API с endpoints `/spaces/{id}/ingest` и `/spaces/{id}/query`
- Интеграция с Qdrant
- Полный пайплайн индексации
- Локальная работа через Ollama

📝 **Следующие шаги (Неделя 3):**

- Multi-tenant архитектура
- Аутентификация
- UI
